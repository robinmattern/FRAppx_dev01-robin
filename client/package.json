{
  "name": "0c-formr-react-apps",
  "version": "0.1.0",
  "private": true,

  "dependencies": {
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "devDependencies": {
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "echo npm ERROR: * You must be in an app folder to start app",
    "test":  "echo npm TEST error: No test specified && exit 1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
